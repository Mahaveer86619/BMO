import pytest
from app.utils.audio import normalize_audio
from io import BytesIO

def test_normalize_audio_output_format():
    # Simple test to verify the function exists and doesn't crash
    # Real audio testing requires pydub/dummy data
    pass
